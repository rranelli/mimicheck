require 'spec_helper'

describe MimiCheck do
  it 'has a version number' do
    expect(MimiCheck::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
